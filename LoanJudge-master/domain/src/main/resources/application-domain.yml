spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/loanjudge?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: loanjudge
    password: test

  redis:
    host: localhost
    port: 6379